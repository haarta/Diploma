server:
  port: ${SERVER_PORT:8081}

spring:
  security:
    user:
      name: disabled
      password: disabled
  datasource:
    url: jdbc:postgresql://${DB_HOST:auth-db}:${DB_PORT:5432}/${DB_NAME:auth_db}
    username: ${DB_USER:auth_user}
    password: ${DB_PASSWORD:auth_password}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: false


auth:
  bootstrap:
    admin-email: ${ADMIN_EMAIL:}
    admin-password: ${ADMIN_PASSWORD:}
  jwt:
    secret: ${AUTH_JWT_SECRET:change_me_to_a_real_secret_change_me}
    access-ttl-minutes: ${AUTH_ACCESS_TTL_MIN:15}
    refresh-ttl-days: ${AUTH_REFRESH_TTL_DAYS:14}
    issuer: ${AUTH_ISSUER:medisystem-auth}
